#include "main.h"
/**
 * _strlen - function for length of a string
 * @s: passing value of str
 * Return: return length of the string
 */

int _strlen(char *s)
{
int c;
while (*s != '\0')
c++;
return (c);
}
